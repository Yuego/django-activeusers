============
Installation
============

At the command line::

    $ easy_install django-activeusers-tmp

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-activeusers-tmp
    $ pip install django-activeusers-tmp